{
  "name": "territory-portal",
  "version": "1.0.0",
  "private": true,
  "description": "Territory Portal — store side app: redemption of credits into brand activations & budtender trainings",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm --prefix web install --no-audit --no-fund && npm --prefix web run build",
    "start": "node api/src/server.js",
    "dev:web": "npm --prefix web run dev"
  }
}