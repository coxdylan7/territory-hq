{
  "name": "territory-hq-api",
  "version": "1.0.0",
  "private": true,
  "description": "Territory HQ REST API (Express + Supabase Postgres)",
  "type": "commonjs",
  "main": "src/server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "dev": "node --watch --env-file-if-exists=.env src/server.js"
  },
  "engines": {
    "node": ">=20.6"
  },
  "dependencies": {
    "express": "^5.1.0",
    "postgres": "^3.4.5"
  }
}